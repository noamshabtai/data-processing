[project]
name = "data-fetcher"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "yfinance",
    "pandas",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/data_fetcher"]
