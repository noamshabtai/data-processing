base:
  model:
    input_dim: 6
    hidden_dim: 32
    output_dim: 1
    num_layers: 2

cases:
  - name: model
    sweep:
      model:
        - num_layers: 1
        - num_layers: 2
      simulation:
        - seq_len: 10
          batch_size: 4
        - seq_len: 20
          batch_size: 8
      training:
        - epochs: 2
          lr: 0.001
