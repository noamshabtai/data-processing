base:
  network:
    input_dim: 6
    hidden_dim: 32
    output_dim: 1
  simulation:
    seq_len: 10
    batch_size: 4

cases:
  - name: num_layers_1
    network:
      num_layers: 1
  - name: num_layers_2
    network:
      num_layers: 2
