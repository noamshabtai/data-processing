base:
  predictor:
    input_buffer:
      channel_shape: []
      step_size: 1
      buffer_size: 50
      dtype: float32
    feature_extraction:
      window_size: 20
    model:
      input_dim: 6
      hidden_dim: 32
      output_dim: 1
      num_layers: 2

cases:
  - name: basic
